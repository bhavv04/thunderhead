services:
  thunderhead:
    build: .
    # Or, once you publish to a registry:
    # image: bhavv04/thunderhead:v0.2.0
    container_name: thunderhead
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=UTC